$(".calendar").fullCalendar(
    "renderEvent",
    $.parseJSON("<%= j render(@event, format: :json).html_safe %>"),
    true
);
$("#new_event").modal("hide");
$("#noti_Container").html('<%= j render("layouts/ring") %>');
initialize_notification();
