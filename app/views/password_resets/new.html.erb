<% provide :title, t(".forgot_password") %>

<div class="container-fluid">
  <div class="row">
    <div class="col-md-8 overview-bgi cnt-bg-photo framgia_background">
      <div class="login-info">
        <h3><%= t ".we" %> </h3>
      </div>
    </div>
    <div class="col-md-4 ">
      <div class="content-form-box">
        <% flash.each do |message_type, message| %>
          <%= content_tag :div, message, class: "alert alert-#{message_type}" %>
        <% end %>
        <h1 class="login-header"><%= t ".forgot_password" %></h1>
        <%= form_for :password_reset, url: password_resets_path do |f| %>
          <div class="form-group">
            <%= f.label :email %>
            <%= f.email_field :email, class: "form-control" %>
          </div>
          <%= f.submit t(".submit_btn"), class: "btn btn-color btn-md btn-block btn-signup" %>
        <% end %>
        <div class="login-footer">
          <div class="pull-left">
            <%= link_to t(".back"), login_path %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
